<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Background animation */
        @keyframes gradientAnimation {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        body {
            background: linear-gradient(-45deg, #1E90FF, #000000, #1E90FF, #000000);
            background-size: 400% 400%;
            animation: gradientAnimation 6s ease infinite;
            color: white;
        }
        
        .btn-primary {
            background-color: #1E90FF;
            transition: background 0.3s, transform 0.2s ease;
        }
        .btn-primary:hover {
            background-color: #187bcd;
            transform: scale(1.05);
        }

        .input-field {
            background-color: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: white;
            transition: all 0.3s ease-in-out;
        }
        .input-field:focus {
            border-color: #1E90FF;
            box-shadow: 0 0 10px rgba(30, 144, 255, 0.5);
        }
        .input-field::placeholder {
            color: rgba(255, 255, 255, 0.5);
        }
        .error-box {
            background-color: #f8d7da; /* Light red background */
            color: #721c24; /* Dark red text */
            border: 1px solid #f5c6cb; /* Light red border */
            padding: 10px;
            margin-bottom: 10px;
            border-radius: 5px;
            width: fit-content;
        }
    </style>
</head>
<body class="h-screen flex items-center justify-center">
    <div class="w-96 bg-gray-900 p-8 rounded-lg shadow-lg">
        <h2 class="text-2xl font-bold text-center mb-6 text-white">Sign In</h2>
        <% if (error) { %> <!-- Check if error exists -->
            <p class="error-box"><%= error %></p> <!-- Display error message -->
        <% } %>
        <form action="http://localhost:2025/console/auth/signin" method="POST">
            <div class="mb-4">
                <label for="email" class="block text-sm font-medium text-gray-300">Email</label>
                <input type="email" id="email" name="email" required 
                    class="w-full px-3 py-2 input-field rounded-lg focus:outline-none focus:ring focus:ring-blue-500">
            </div>
            <button type="submit" 
                class="w-full btn-primary text-white py-2 rounded-lg hover:shadow-lg transition">
                Sign In
            </button>
        </form>
        <p class="text-sm text-center text-gray-400 mt-4">
            Don't have an account? <a href="#" class="text-blue-400 hover:underline">Sign Up</a>
        </p>
    </div>
</body>
</html>
